%h1 Signs
%table
  %tr
    %th Title
    %th Active Slides
    %th Last Check In
    %th.actions Actions
  -@signs.each do |sign|
    %tr
      %td=link_to(sign.title, sign)
      %td&=sign.active_slides.length
      %td&=sign.last_check_in.to_s(:long) if sign.checked_in?
      %td=render(:partial=>'shared/action_links', :locals=>{:object=>sign})
=render('index_aside')
